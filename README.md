# knutcut-releases

Self-update binaries for Knutcut. Upload the new `Knutcut-v<ver>-release.apk` here and bump `latest.json` (`versionCode`, `versionName`, `apk`). The app reads `latest.json` on start and offers the update.
